[project]
name = "alloy-api"
version = "0.1.0"
description = "FastAPI backend"
requires-python = ">=3.14"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30"]

[dependency-groups]
dev = ["httpx>=0.28"]

[build-system]
requires = ["uv_build>=0.12.1,<1"]
build-backend = "uv_build"
