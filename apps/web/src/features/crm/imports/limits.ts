/**
 * The API's default `ALLOY_IMPORT_MAX_BYTES`. Checked here before an upload
 * starts, so the user hears about it at once; the API is the authority.
 */
export const IMPORT_MAX_BYTES = 10 * 1024 * 1024;
